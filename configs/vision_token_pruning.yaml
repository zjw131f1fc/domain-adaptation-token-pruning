# Attention Consistency Pruning 配置文件
# 基于 attention 聚合一致性的视觉 token 剪枝

global_settings:
  seed: 42
  device: "cuda"
  dtype: "bfloat16"
  pytorch_cuda_alloc_conf: "expandable_segments:True"
  dataset_cache_dir: "/data/users/zjw/dataset_cache"
  hf_cache_dir: "/data/users/zjw/huggingface_cache"
  save_dir: "./outputs/checkpoints"
  log_dir: "./outputs/logs"
  # Checkpoint 路径（用于恢复训练或评估）
  # 训练时：如果指定则从该 checkpoint 恢复训练
  # 评估时：必须指定，否则报错（也可通过命令行 --checkpoint 覆盖）
  checkpoint: null # 例如: "./outputs/checkpoints/checkpoint_final.pt"

trainer_settings:
  dl_settings:
    epochs: 1
    batch_size: 5
    # 序列最大长度（LLaVA 1.5: 576 vision tokens + 文本）
    # ScienceQA 等长文本数据集建议 2048，VQAv2 等短文本可用 1024
    max_length: 1024
    optimizers:
      layer_pruners:
        type: "adam"
        lr: 5.0e-05  # 提高学习率，让 pruner 更快学习
        weight_decay: 0.01  # 防止过拟合
      discriminator:
        type: "adam"
        lr: 2.0e-04
      # 学习率调度器配置
      lr_scheduler:
        type: "cosine"  # 'none', 'cosine', 'linear'
        warmup_ratio: 0.1  # warmup 步数占总步数的比例
        min_lr_ratio: 0.1  # 最小学习率 = 初始学习率 * min_lr_ratio
    print_loss_every_batches: 40  # 隔多久打印一次损失
    eval_every_batches: 5000  # 隔多久评估一次
    eval_max_samples: 20000000 # 每次评估使用多少数据
    save_every_batches: 5000 # 隔多久保存一个checkpoint
    grad_clip_max_norm: null # 不要开

dataset_settings:
  name: "vqa-vqav2"
  split:
    train: 5000
    test: 100000
  fast_load_no_random: false
  category_priority:
    enable: true
    values:
      - {train: 'mean'}    # 训练集均衡各类别数量（防止过拟合到高频类别）
      # - {test: 'mean'}   # 测试集保持原始比例

backbone_settings:
  name: "llava-1.5-7b"

method_settings:
  # ==================== 剪枝层配置 ====================
  # 剪枝器和判别器是配套的，每个剪枝层都有一个判别器
  pruning_layers: [4,14,24]

  # ==================== Pruner 配置 ====================
  # CrossAttentionPruner: 使用可学习 queries 评估 vision tokens 重要性
  pruner_d_internal: 512   # 内部特征维度
  pruner_n_heads: 4        # Cross-attention 头数
  pruner_dropout: 0.1

  # Gumbel noise 模式配置
  # - "always": 始终使用 Gumbel noise（有随机性，训练和推理可能不一致）
  # - "never": 始终不使用 Gumbel noise（纯 STE，训练和推理完全一致）
  # - "hybrid": 混合三阶段策略（推荐）
  #     阶段1 探索期 (0% - phase1_end): 温度退火 + Gumbel noise，探索哪些 token 重要
  #     阶段2 稳定期 (phase1_end - phase2_end): 低温 + noise_scale 从 1.0 退火到 0
  #     阶段3 确定性微调期 (phase2_end - 100%): noise=0，训练=推理，消除 gap
  gumbel_mode: "hybrid"

  # 混合模式三阶段配置（仅 gumbel_mode="hybrid" 时生效）
  # 阶段1: temp 从 start 退火到 end, noise_scale=1.0
  # 阶段2: temp 固定为 end, noise_scale 从 1.0 平滑退火到 0
  # 阶段3: temp 固定, noise_scale=0 (确定性决策)
  hybrid_phase1_end: 0.33    # 探索期结束点（占总步数比例）
  hybrid_phase2_end: 0.66    # 稳定期结束点（占总步数比例）
  hybrid_phase1_temp_start: 1.5   # 探索期起始温度
  hybrid_phase1_temp_end: 0.3     # 探索期结束温度（也是稳定期温度）
  hybrid_phase3_temp: 0.3         # 确定性微调期温度

  # ==================== Discriminator 配置 ====================
  # 每个 answer token 独立判别
  disc_d_d: 512
  disc_dropout: 0.3
  disc_use_spectral_norm: true  # 谱归一化（提高训练稳定性）
  disc_loss_type: hinge            # 损失类型: bce, wgan, hinge
  disc_gp_weight: 10.0           # WGAN-GP 梯度惩罚权重

  # 判别器重初始化配置
  disc_reinit_enable: true       # 是否启用判别器重新初始化
  disc_reinit_mode: random     # 重初始化模式: threshold(准确率阈值) 或 random(随机概率)
  disc_reinit_threshold: 0.97     # threshold 模式：准确率超过此阈值时重新初始化
  disc_reinit_prob: 0.001          # random 模式：每步重初始化的概率

  # ==================== Adapter 配置 ====================
  # Adapter 补偿剪枝后的信息损失
  # - simple: 简单 MLP adapter
  # - lightweight: Mask-Aware FiLM adapter（轻量级，根据剪枝 pattern 动态调整）
  adapter_type: lightweight
  adapter_bottleneck: 512         # adapter 瓶颈维度

  # ==================== Pruner Warmup ====================
  pruner_warmup_ratio: 0.05  # 前 10% 步数不做对抗训练

  # ==================== 剪枝目标 ====================
  target_token_num: 128  # 目标保留的 vision token 数量
  # 剪枝目标退火：从 100% 保留逐渐退火到 target_token_num
  # 0.0 = 不退火，直接使用目标值
  # 0.3 = 前 30% 训练步数进行余弦退火
  sparsity_anneal_ratio: 0.2

  # ==================== Temperature 配置 ====================
  # 注意：如果使用 gumbel_mode="hybrid"，温度由 hybrid_phase* 参数控制
  # 以下配置仅在 gumbel_mode="always" 或 "never" 时生效
  temperature: 1.5
  temperature_min: 0.1
  temperature_anneal_rate: 0.5

  # ==================== 损失权重 ====================
  # task_loss: Cross entropy on answer tokens
  # adv_loss: 让 fake 被判为 real
  # sparsity_loss: 约束保留率到目标值

  task_loss_weight: 1.5
  adv_loss_weight: 0.5
  sparsity_weight: 0.8   # 提高到 5.0，让 sparsity loss 有足够影响力
  entropy_weight: 0.6    # Entropy 正则：鼓励 logits 生成极端值，使训练和推理行为一致
  uniformity_weight: 0 # 均匀性损失：鼓励各层增量剪枝率相近，避免只有第一层剪枝
  atp_weight: 0.05  # L_ATP 损失：强制深层网络剪掉更多令牌，L_ATP = sum(mask.sum()/576 * layer_idx)
  detach_mask_for_adv: false  # 是否对 adv_loss 阻断 pruner 的梯度（只传到 adapter）
# 64.81 27.11
  # 动态权重调度 (可选)
  loss_weight_warmup_ratio: 0.0  # 0 表示不使用 warmup
  task_loss_weight_start: 1.0
  adv_loss_weight_start: 0.7

  # Sparsity warmup - 启用，逐渐增加 sparsity 约束
  sparsity_warmup_enable: false
  sparsity_warmup_ratio: 0.3  # 前 30% 训练步数进行 warmup
  sparsity_weight_max: 0.8   # warmup 结束后达到 10.0

evaluation_settings:
  eval_mode: ["hard"]
