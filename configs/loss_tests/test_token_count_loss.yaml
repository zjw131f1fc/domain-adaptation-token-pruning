# 测试 Token Count Loss 收敛性
# 只启用 token_count_loss，其他 loss 权重为 0

method_settings:
  token_count_loss_weight: 1.0

global_settings:
  save_dir: "./outputs/loss_tests/token_count_loss"
  log_dir: "./outputs/loss_tests/token_count_loss/logs"
