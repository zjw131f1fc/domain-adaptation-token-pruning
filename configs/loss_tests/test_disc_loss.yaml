# 测试 Discriminator Loss 收敛性
# 启用 adv_loss 以产生 fake samples，观察 disc_loss 是否收敛

method_settings:
  adv_loss_weight_start: 1.0
  adv_loss_weight: 1.0

global_settings:
  save_dir: "./outputs/loss_tests/disc_loss"
  log_dir: "./outputs/loss_tests/disc_loss/logs"
