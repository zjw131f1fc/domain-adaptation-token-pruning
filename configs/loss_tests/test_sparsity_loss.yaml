# 测试 Sparsity Loss 收敛性
# 只启用 sparsity_loss，其他 loss 权重为 0

method_settings:
  sparsity_weight: 1.0
  sparsity_weight_max: 1.0

global_settings:
  save_dir: "./outputs/loss_tests/sparsity_loss"
  log_dir: "./outputs/loss_tests/sparsity_loss/logs"
