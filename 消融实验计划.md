# ECCV 消融实验清单

只列“需要做哪些实验”，不写协议与细节。

---

## 1) 主表（同一保留 token 预算）

- Ours（Full）vs 其他方法（同 K）

---

## 2) 组件消融（核心）

- Full
- w/o pruner（top-k-attn）
- w/o adapter（无 delayed repair）
- w/o repair loss（`λ_repair=0`，保留 delayed repair 结构）
- mean-only vs mean+var（`repair_var_weight α=0` vs 默认）

---

## 3) Adapter 结构（可选）

- Static MLP
- FiLM

---

## 4) Repair 设计（机制）

- Repair 插入次数：0 段 / 1 段 / 3 段（默认）
- Repair 插入层平移（稳健性）：默认 repair layers vs `+1` vs `-1`（其余设置不变）
- Repair 作用范围：gen_answer-only（默认） vs all-tokens

---

## 5) Pruner 设置

- Pruner 数量：Only L4 / L4+L14 / L4+L14+L24
- Pruner 插入层平移（稳健性）：默认 pruning layers vs `+1` vs `-1`（其余设置不变）

---

## 6) 剪枝行为统计

- 不同 K 下，各 pruning layer 的实际 kept/pruned 百分比（逐层统计）

---

## 7) 效率对比

- FLOPs / CUDA time / Peak memory：Ours vs 其他方法（同 K）

---

## 8) 分析图（你那两张）

- Repair layers 表示距离（A: teacher+adapter vs B: no-teacher,no-adapter；建议改图方式如下）
  - y 轴从 0 开始（distance 不应出现“负下界”的视觉误导）
  - 误差条用 95% CI / standard error（不要用 std 直接画到 0 以下）
  - 在 caption 写清：距离定义（cosine distance）、“pooled”怎么做、统计的 token 区域（只取 gen_answer）、样本量 N（图像/问题数）与 seed 数
  - 同时报告“相对改进”：对每个样本计算 Δ = dist(B) − dist(A)，画一张 paired scatter/box（或在柱状图上标注平均 Δ）
- 表示分布可视化（如 1D LDA，real vs A vs B）

---

## 9) 超参数消融（尽量少，但够 ECCV）

**必做（建议放主文或附录，二选一即可）**
- `repair_loss_weight (λ_repair)`：`{0, default, 2×default}`（固定 K）
- `repair_var_weight (α)`：`{0, default}`（固定 K 和 λ_repair）

**可选（若你们还想再加 1 条“稳定性”证据，放附录即可）**
- `repair_detach_input`：默认 vs 取反（固定 K、λ_repair、α）

**一般不需要单独做（主表已同 K；若训练稳定且能达标，可不扫）**
- `sparsity_weight (λ_sparse)`
- `sparsity_anneal_ratio`

**具体做法（不展开写协议，只给最小操作）**
- 固定一个代表性预算：例如 `K=64`（或你们主表最常用的 K）
- 每次只改一个超参，其他保持 Full 默认
- 每个 setting 至少跑 2 个 seed，报告均值（可带误差条）
- 同时报告实际 kept ratio（确保仍是“同 K/同预算”的对比）

---

## 10) Trade-off 曲线（Accuracy vs Compute/Latency）

- 选 3–4 个不同的保留预算 K（例如 `K∈{32,64,96,128}`，用你们已有的点即可）
- 对比两条曲线（同一评估设置）：
  - Pruning-only（无 delayed repair）
  - Pruning+Repair（Full）
- 横轴：CUDA time 或 FLOPs（选你们已有/最可靠的一个）
- 纵轴：主指标（accuracy/score）
