# 0201 Adapter Query 条件分析

## 问题背景

在 LightweightAdapter 中，设计了 Query-Aware 的 FiLM 调制机制，但发现训练和推理时存在不一致：
- **训练时**：传入了 query（但 adapter 内部的 query 使用代码被注释掉了）
- **推理时**：没有传入 query

## 代码位置

- Adapter 定义：`method/models/adapter.py` - `LightweightAdapter`
- 训练时调用：`method/models/prunable_llama_layer.py:287-291`
- 推理时调用：`method/models/prunable_llava.py:804-808`（Prefill）和 `1035-1039`（Decode）

## 尝试过程

### 尝试1：启用推理时的 query

**修改**：
- Prefill 阶段：`query_states_flat = query_states`
- Decode 阶段：`query=query_states_flat_gen`
- adapter.py：取消注释 query 使用代码

**结果**：acc 降到 5%

### 调试分析

添加调试代码对比训练和推理时的输入：

| 指标 | 训练 (step 0) | 推理 (step 200) | 差异 |
|------|---------------|-----------------|------|
| x.std | 0.12 ~ 0.14 | 0.02 ~ 0.024 | **6倍** |
| mask.mean | 0.48 ~ 0.49 | 0.18 ~ 0.26 | **2倍** |
| query.std | 1.32 | 1.27 | 一致 |
| query[:, 0, :5] | 各样本不同 | 各样本不同 | 一致（不同样本） |
| query_emb.std | 0.17 | 0.58 ~ 0.61 | **3.5倍** |

**关键发现**：
1. **query 输入是一致的**：计算方式相同（`q_proj(hidden_normed)`），值不同是因为不同样本
2. **query_emb 差异大**：因为 query_proj 权重在训练过程中被更新了
3. **x.std 差异大**：训练用 soft mask，推理用 hard mask
4. **mask.mean 差异大**：训练保留 48%，推理保留 20%

### 尝试2：推理时禁用 query

**修改**：
- Prefill 阶段：`query_states_flat = None`
- Decode 阶段：`query=None`

**结果**：恢复正常，但没有利用 query 信息

### 尝试3：训练时用同分布噪声代替 query（最终方案）

**思路**：
- 问题根源：训练时 adapter 学习了依赖具体 query 值的模式
- 推理时 query 分布与训练时不同（因为权重更新、数据不同等）
- 解决方案：训练时不让 adapter 学习依赖具体 query 值

**修改**（adapter.py）：
```python
if query is not None:
    # 训练时：用同分布噪声代替 query，让 adapter 不依赖具体的 query 值
    if self.training:
        query_noise = torch.randn_like(query) * query.std() + query.mean()
        query_emb = self.query_proj(query_noise)
    else:
        query_emb = self.query_proj(query)
    condition = condition + query_emb
```

**结果**：效果很好！

## 原理分析

### 为什么同分布噪声有效？

1. **保留统计特性**：噪声与 query 有相同的 mean 和 std，保留了 query 的统计信息
2. **消除具体依赖**：adapter 无法学习依赖具体的 query 模式（因为每次都是随机的）
3. **泛化能力强**：推理时即使 query 分布略有不同，adapter 也能正常工作

### 训练-推理一致性

| 阶段 | query 输入 | query_proj 输入 |
|------|-----------|-----------------|
| 训练 | 真实 query | 同分布噪声 |
| 推理 | 真实 query | 真实 query |

虽然训练和推理时 query_proj 的输入不同，但由于训练时使用的是同分布噪声，adapter 学习的是基于 query 统计特性的补偿，而不是具体的 query 模式。这使得推理时即使使用真实 query，也能正常工作。

## 总结

1. **问题**：直接启用 query 导致训练-推理不一致，acc 降到 5%
2. **原因**：adapter 学习了依赖具体 query 值的模式，推理时 query 分布不同
3. **解决方案**：训练时用同分布噪声代替 query
4. **效果**：保留了 query 的统计信息，同时避免了过拟合到具体的 query 模式

## 相关 Commit

- `de36c11` fix: 修复 Adapter mask 传递问题，启用 query 条件
- `3cf2eb6` feat: 添加 Adapter 调试代码，启用推理时 query 条件
- `217de2f` refactor: 移除 Adapter 中的 Dropout
- `06f3c83` fix: 推理时禁用 query，移除调试代码
