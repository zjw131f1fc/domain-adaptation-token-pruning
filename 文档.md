# Attention Consistency Pruning
## 1. 核心研究问题与动机

### 1.1 问题定义

**目标：** 对多模态大语言模型（mLLM，如 LLaVA）进行 vision token 剪枝，在保持性能的同时减少推理计算量。

**挑战：** 现有剪枝方法性能下降明显，因为：

- Vision tokens 被剪枝后，LLM 通过 cross-attention 聚合得到的表示向量发生了**分布偏移**
- LLM 的推理部分（后续层）在预训练/微调时适应的是**完整 vision tokens 的聚合结果**
- 剪枝后的聚合结果与 LLM 期望的输入分布不匹配，导致性能下降

### 1.2 核心洞察

**我们的观察：**

1. 剪枝前后，LLM 的推理部分（Decoder layers 的 MLP、LayerNorm 等）保持不变
2. 但 answer tokens 通过 attention 机制查询 vision tokens 得到的**聚合向量** $\mathbf{h} = \sum_i \alpha_i \mathbf{v}_i$ 的分布改变了
3. 从分布的角度看，这是一个**域适应（Domain Adaptation）**问题：
    - **源域（Source）：** 完整 vision tokens 的聚合结果 $\mathbf{h}_{\text{real}}$
    - **目标域（Target）：** 剪枝后 vision tokens 的聚合结果 $\mathbf{h}_{\text{fake}}$

**关键区分：**

- **信息丢失**：某些语义被彻底剪掉（无法恢复）
- **分布偏移**：保留的 tokens 的表示方式改变了（**可以通过学习对齐**）

现有启发式方法（如均匀取token、保留 特殊位置 token）在一定程度上是为了**控制分布偏移的幅度**。我们的方法更进一步：主动学习如何**最小化分布偏移**并**补偿残余偏移**。

---

## 2. 方法框架（ACP）

### 2.1 整体架构

我们提出 **Attention Consistency Pruning (ACP)**，包含三个核心模块：

```
┌─────────────────────────────────────────────────────────────┐
│                    Frozen LLaVA Model                       │
│  Vision Encoder → Projector → LLaMA Decoder (32 layers)    │
└─────────────────────────────────────────────────────────────┘
                              ↓
              在第 L₁, L₂, L₃ 层（如 4, 14, 24）插入剪枝机制
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  Prunable Decoder Layer（替换原始 layer）                   │
│                                                             │
│  1. Cross-Attention Pruner:                                 │
│     - 输入：vision hidden states + Q→V attention baseline   │
│     - 输出：s_i（每个 token 的保留得分）                    │
│     - 训练：Gumbel-Softmax soft mask                        │
│     - 推理：hard threshold                                  │
│                                                             │
│  2. Dual Aggregation（仅在剪枝层）:                         │
│     - attn_real = Σ α_i v_i          （完整聚合）           │
│     - attn_fake = Σ (α_i · m_i) v_i  （剪枝后聚合）         │
│                                                             │
│  3. Pruning Adapter（作用在整个序列的 attention output）:   │
│     - 输入：attn_fake + pruning mask + attention query      │
│     - 输出：attn_corrected（修正后的 attention output）     │
│     - 设计：Mask-Aware + Query-Aware FiLM modulation        │
│                                                             │
│  4. Layer Discriminator（仅用于 answer 位置）:              │
│     - 从 attn_real/attn_fake 提取 answer 位置得到 h_real/h_fake │
│     - 输入：h ∈ {h_real, h_fake}                            │
│     - 输出：D(h)（判别真假的概率）                          │
└─────────────────────────────────────────────────────────────┘
```

**关键设计说明：**

- **单路前向传播**：训练时只使用剪枝后的路径（attn_fake）进行后续层计算，避免双路前向的计算开销
- **局部双路构建**：仅在剪枝层同时构建 attn_real 和 attn_fake，从中提取 answer 位置用于对抗学习
- **级联剪枝**：每层的 pruner 和 adapter 学习的是**在前序层剪枝结果下**的最优策略

---

### 2.2 核心组件详解

#### 2.2.1 Cross-Attention Pruner

**设计思想：** 使用可学习的 pruning queries 通过 cross-attention 评估每个 vision token 的重要性。采用残差设计，以 LLM 自身的 attention 作为 baseline，pruner 学习修正量。

**数学表述：**

设 vision tokens 的隐状态为 $\mathbf{V} = [\mathbf{v}_1, \ldots, \mathbf{v}_N] \in \mathbb{R}^{N \times d}$，question-to-vision attention weights 为 $\mathbf{A}_{\text{q2v}} \in \mathbb{R}^{N}$。

1. **Baseline 重要性（基于 LLM 自身 attention）：**

    将 attention 权重转换到 logit 空间并中心化：
    $$b_i = \log \mathbf{A}_{\text{q2v}}[i] - \frac{1}{N}\sum_{j=1}^N \log \mathbf{A}_{\text{q2v}}[j]$$

2. **Vision Token 投影：**

    $$\mathbf{V}' = \text{Linear}(\mathbf{V}) \in \mathbb{R}^{N \times d'}$$

    其中 $d'$ 为内部维度（如 512）。

3. **Multi-Head Cross-Attention：**

    使用 $K$ 个可学习 pruning queries $\mathbf{Q}_p \in \mathbb{R}^{K \times d'}$（默认 $K=4$），通过多头 cross-attention 计算每个 query 对 vision tokens 的注意力：

    $$\mathbf{A}_p = \text{MultiHeadAttn}(\mathbf{Q}_p, \mathbf{V}', \mathbf{V}') \in \mathbb{R}^{K \times N}$$

    其中 MultiHeadAttn 内部对 Q/K/V 都有投影层，使用 $H$ 个 attention heads（默认 $H=4$）。

    聚合多 query 的注意力权重：
    $$\Delta_i^{(1)} = \text{Linear}(\mathbf{A}_p[:, i]) \in \mathbb{R}$$

4. **Token-level 特征：**

    对每个 token 独立评分：
    $$\Delta_i^{(2)} = \text{MLP}_{\text{token}}(\mathbf{V}'[i])$$

    其中 MLP 结构为：$\text{Linear} \to \text{LayerNorm} \to \text{GELU} \to \text{Dropout} \to \text{Linear}$

5. **残差设计得到最终保留得分：**

    $$s_i = b_i + \Delta_i^{(1)} + \Delta_i^{(2)} + c$$

    其中 $c$ 是可学习的偏置项（`keep_bias`）


**训练/推理分离：**

- **训练阶段（软剪枝）：** 使用 Gumbel-Softmax 生成可微的 hard mask：
    $$m_i = \text{GumbelSoftmax}([0, s_i], \tau)[1]$$

    其中 $\tau$ 是温度参数，从 1.0 退火到 0.1。输出为 0/1 的 hard mask，但梯度通过 straight-through estimator 传播。

- **推理阶段（硬剪枝）：** 使用硬阈值：
    $$m_i = \mathbb{1}[\sigma(s_i) > \theta]$$

    其中 $\sigma$ 是 sigmoid 函数，$\theta$ 是阈值（默认 0.5）。

**参数量：** 约 3.4M × 3 层 = 10.2M

---

#### 2.2.2 Lightweight Adapter

**设计思想：** Adapter 作为一个轻量级层，作用在**整个序列的 attention output** 上，根据 pruning mask 和 attention query 动态修正剪枝带来的分布偏移：

- 不同 pruning mask 导致不同程度的信息缺失
- 不同 token 的 attention query 反映了不同的查询意图

因此，adapter 需要**根据 pruning mask 和当前 token 的 query 动态调整修正策略**。

**数学表述：**

设剪枝后的 attention output 为 $\mathbf{X} \in \mathbb{R}^{T \times d}$（$T$ 为序列长度），pruning mask 为 $\mathbf{m} \in \{0,1\}^N$，attention query 为 $\mathbf{Q} \in \mathbb{R}^{T \times d}$。

1. **Mask Encoding：** 编码哪些 tokens 被剪除

    $$\mathbf{e}_m = \text{MLP}_{\text{mask}}(\mathbf{m}) \in \mathbb{R}^{d_b}$$

    其中 MLP 结构为：$\text{Linear}(N \to d_b) \to \text{GELU} \to \text{Dropout} \to \text{Linear}(d_b \to d_b)$

2. **Query Encoding：** 投影 attention query 到 bottleneck 维度

    $$\mathbf{E}_q = \text{Linear}_{\text{query}}(\mathbf{Q}) \in \mathbb{R}^{T \times d_b}$$

3. **Condition Fusion：** 融合 mask（全局）和 query（per-token）信息

    $$\mathbf{C} = \mathbf{e}_m + \mathbf{E}_q \in \mathbb{R}^{T \times d_b}$$

    其中 $\mathbf{e}_m$ 广播到每个 token 位置。

4. **FiLM Modulation：** 根据条件生成 per-token 变换参数

    $$\boldsymbol{\Gamma} = 1 + \text{Linear}_{\gamma}(\mathbf{C}) \in \mathbb{R}^{T \times d_b}$$
    $$\boldsymbol{B} = \text{Linear}_{\beta}(\mathbf{C}) \in \mathbb{R}^{T \times d_b}$$

5. **条件变换（per-token）：**

    $$\mathbf{h} = \text{Dropout}(\text{GELU}(\mathbf{W}_{\text{down}} \mathbf{X})) \in \mathbb{R}^{T \times d_b}$$
    $$\mathbf{h}' = \boldsymbol{\Gamma} \odot \mathbf{h} + \boldsymbol{B}$$
    $$\mathbf{X}' = \mathbf{X} + \mathbf{W}_{\text{up}} \cdot \text{Dropout}(\mathbf{h}')$$

    其中 $\odot$ 表示逐元素乘法。

**初始化策略：**

- $\text{Linear}_{\text{query}}$ 初始化为小值（xavier_uniform, gain=0.1）
- $\boldsymbol{\gamma}$ 网络初始化为全 0（使初始 $\gamma = 1$）
- $\boldsymbol{\beta}$ 网络初始化为全 0
- $\mathbf{W}_{\text{up}}$ 初始化为小值（xavier_uniform, gain=0.1）
- 确保初始时 adapter 为恒等映射，不破坏预训练权重

**参数量：** 约 7.4M × 3 层 = 22M

---

#### 2.2.3 Layer Discriminator

**设计思想：** 通过对抗学习，让 adapter 修正后的 attention output 在分布上接近完整聚合的结果。Discriminator 仅在 **生成 answer token 的位置** 进行判别，提供细粒度监督。

**h_real 和 h_fake 的提取：**

从 attention output 中提取生成 answer 位置的聚合结果：
- $\mathbf{h}_{\text{real}} = \mathbf{A}_{\text{real}}[\text{answer\_positions}]$ - 完整聚合在生成 answer 位置的结果
- $\mathbf{h}_{\text{fake}} = \mathbf{A}_{\text{fake}}[\text{answer\_positions}]$ - 剪枝后聚合（经 Adapter 修正）在 answer 位置的结果

**数学表述：**

设聚合向量 $\mathbf{h} \in \mathbb{R}^{H \times T_a \times d_h}$（$H$ 为 attention heads 数量，$T_a$ 为 answer token 数量，$d_h$ 为 head dimension）。

1. **判别网络：** $$\mathbf{f} = \text{Flatten}(\mathbf{h}) \in \mathbb{R}^{T_a \times (H \cdot d_h)}$$ $$D(\mathbf{h}) = \text{MLP}_{\text{disc}}(\mathbf{f}) \in \mathbb{R}^{T_a}$$

    其中 MLP 结构为：$H \cdot d_h \to 512 \to 256 \to 1$
    

**关键设计：**

- **Per-token 判别：** 每个 answer token 的聚合结果独立判别，提供细粒度监督
- **谱归一化（Spectral Normalization）：** 对 MLP 权重施加谱归一化，稳定训练
- **Hinge Loss：** 使用 hinge loss 替代交叉熵，提高稳定性

**参数量：** 约 2.4M × 3 层 = 7.1M

---

### 2.3 训练流程

#### 2.3.1 损失函数

我们同时优化 4 个损失：

|损失|作用|数学形式|权重|
|---|---|---|---|
|**Task Loss**|保持生成质量|$\mathcal{L}_{\text{task}} = -\sum_t \log p(y_t \mid \mathbf{h}_{\text{fake}})$|$\lambda_1 = 1.3$|
|**Adversarial Loss**|Pruner+Adapter 骗过 Discriminator|$\mathcal{L}_{\text{adv}} = -\mathbb{E}[\log D(A(\mathbf{h}_{\text{fake}}))]$|$\lambda_2 = 0.1$|
|**Discriminator Loss**|区分 real vs fake|$\mathcal{L}_{\text{disc}} = \mathbb{E}[\max(0, 1-D(\mathbf{h}_{\text{real}}))] + \mathbb{E}[\max(0, 1+D(A(\mathbf{h}_{\text{fake}})))]$|$\lambda_3 = 1.0$|
|**Sparsity Loss**|约束保留率到目标值|$\mathcal{L}_{\text{sparse}} = \left\|\bar{r} - r_{\text{target}}\right\|$|$\lambda_4 = 0.5$|

其中 $A(\cdot)$ 表示 Adapter，$D(\cdot)$ 表示 Discriminator。

**Sparsity Loss 详解：**

考虑分层剪枝的累积效应。设共有 $L$ 层（如 32），在第 $\ell_1, \ell_2, \ell_3$ 层进行剪枝（如 4, 14, 24），每层的**独立保留率**分别为 $p_1, p_2, p_3$。

则各层的**累积保留率**为：

- 第 $\ell_1$ 层之后：$r_1 = p_1$
- 第 $\ell_2$ 层之后：$r_2 = p_1 \cdot p_2$
- 第 $\ell_3$ 层之后：$r_3 = p_1 \cdot p_2 \cdot p_3$

受各剪枝层影响的层数分别为：

- $n_0 = \ell_1$ （剪枝前的层，保留率=1）
- $n_1 = \ell_2 - \ell_1$ （第 $\ell_1$ 层影响的层数）
- $n_2 = \ell_3 - \ell_2$
- $n_3 = L - \ell_3$

**精确的加权平均保留率：**
$$\bar{r} = \frac{1}{L} (n_0 \cdot 1 + n_1 \cdot r_1 + n_2 \cdot r_2 + n_3 \cdot r_3)$$

**问题：** 直接约束 $\bar{r}$ 会导致梯度不均匀。由于 $p_1$ 出现在所有乘积项中，其梯度远大于 $p_3$（比例约 14:3.7:1），导致第一个剪枝器压力过大。

**调和平均近似方案：**

为了均匀化各剪枝器的梯度，使用调和平均近似：

1. 从累积保留率计算独立保留率：
   $$p_1 = r_1, \quad p_2 = r_2/r_1, \quad p_3 = r_3/r_2$$

2. 计算调和平均：
   $$h = \frac{3}{1/p_1 + 1/p_2 + 1/p_3}$$

3. 近似加权平均保留率：
   $$\bar{r}_{\text{approx}} = \frac{1}{L} (n_0 \cdot 1 + n_1 \cdot h + n_2 \cdot h^2 + n_3 \cdot h^3)$$

4. Sparsity loss：
   $$\mathcal{L}_{\text{sparse}} = |\bar{r}_{\text{approx}} - r_{\text{target}}|$$

**梯度改善：** 调和平均方案的梯度比例约为 4:1.4:1，比原方案的 14:3.7:1 均匀很多。

**近似误差：** 当 $p_1 < p_2 < p_3$ 时（早期层剪得多），误差约 7-18%。可通过调整 `pruning_threshold` 参数补偿。

**阈值补偿：**

推理时使用 $\sigma(s_i) > \theta$ 判断是否保留（$\sigma$ 为 sigmoid）。默认 $\theta = 0.5$，可在配置中调整：
- $\theta > 0.5$：更严格，保留更少 token，补偿近似高估
- $\theta < 0.5$：更宽松，保留更多 token

这个设计确保：

- 各剪枝器的梯度更均匀，避免早期层压力过大
- 考虑了剪枝的累积效应
- 可通过阈值微调补偿近似误差

#### 2.3.2 优化策略

**参数分组：**

- **Generator 参数：** $\theta_G = {\theta_{\text{pruner}}, \theta_{\text{adapter}}}$
- **Discriminator 参数：** $\theta_D$

**优化器：**

- Pruner + Adapter：AdamW，学习率 $2 \times 10^{-5}$
- Discriminator：AdamW，学习率 $1 \times 10^{-4}$

**总损失：** $$\min_{\theta_G} \max_{\theta_D} \quad \lambda_1 \mathcal{L}_{\text{task}} + \lambda_2 \mathcal{L}_{\text{adv}} + \lambda_4 \mathcal{L}_{\text{sparse}} + \lambda_3 \mathcal{L}_{\text{disc}}$$

#### 2.3.3 Temperature Annealing

Gumbel-Softmax 温度退火策略（前 80% 训练步数）：

$$\tau(t) = \max\left(1.0 \cdot \left(\frac{0.1}{1.0}\right)^{t / (0.8 T)}, 0.1\right)$$

其中 $t$ 为当前步数，$T$ 为总步数。

**效果：**

- 高温阶段：软 mask，探索不同剪枝策略
- 低温阶段：接近硬 mask，逼近推理行为

#### 2.3.4 训练稳定性措施

对抗训练容易出现模式崩溃，我们采用以下措施：

1. **谱归一化：** 限制 discriminator 的 Lipschitz 常数 $$\mathbf{W}_{\text{SN}} = \frac{\mathbf{W}}{\sigma(\mathbf{W})}$$ 其中 $\sigma(\mathbf{W})$ 是最大奇异值
    
2. **Hinge Loss：** 相比交叉熵更稳定
    
3. **随机重启：** Discriminator 每步以 0.1% 概率重新初始化
    
4. **梯度裁剪：** 限制梯度范数不超过 1.0
    
5. **权重调优：** $\lambda_2 = 0.1$ 是实验调优的平衡点
    

---

### 2.4 推理流程

#### 2.4.1 Hard Pruning

训练时使用 Gumbel-Softmax（软 mask），推理时使用**物理删除**：

**Prefill 阶段（逐层删除）：**

对于剪枝层 $\ell \in {\ell_1, \ell_2, \ell_3}$：

1. 计算保留 mask： $$m_i = \mathbb{1}[\sigma(s_i) > \theta_\ell]$$
    
2. 物理删除 tokens： $$\mathbf{V}^{(\ell+1)} = \mathbf{V}^{(\ell)}[\mathbf{m}]$$
    
    其中 $[\mathbf{m}]$ 表示按 mask 索引。
    
3. 更新 position embeddings 和 KV cache
    

**Decode 阶段：**

- 正常自回归生成（vision tokens 已被删除）
- 每步只生成一个新 token，不再触发剪枝

**效果：**

- 真正减少计算量（FLOPs 和显存）
- 推理时不需要 discriminator

---

## 3. 关键技术特点总结

### 3.1 单路前向传播

**训练策略：**

- 仅在剪枝层同时构建 $\mathbf{h}_{\text{real}}$ 和 $\mathbf{h}_{\text{fake}}$
- 后续层只使用 $\mathbf{h}_{\text{fake}}$ 进行前向传播
- 避免双路前向的计算开销（约节省 50% 训练时间）

**学习目标：**

- 每层的 pruner 和 adapter 学习在**当前剪枝状态下**的最优策略
- 级联优化：后层的模块适应前层的剪枝结果

### 3.2 残差设计

所有可学习模块均采用残差连接：

**Pruner：** $$s_i = \underbrace{b_i}_{\text{baseline}} + \underbrace{\Delta_i}_{\text{learned correction}}$$

**Adapter：** $$\mathbf{h}' = \mathbf{h}_{\text{fake}} + \underbrace{f(\mathbf{h}_{\text{fake}}, \mathbf{c})}_{\text{learned correction}}$$

**优势：**

- 初始化为恒等映射，不破坏预训练权重
- 梯度流动更顺畅
- 训练更稳定

### 3.3 多层级剪枝

在 LLaMA decoder 的不同深度（如 L4, L14, L24）插入剪枝模块：

**动机：**

- 浅层：捕捉低级视觉特征的冗余
- 中层：捕捉中级语义的冗余
- 深层：捕捉高级推理的冗余

**级联效应：**

- 前层剪枝影响后层的输入分布
- 后层的 adapter 需要同时补偿前序所有剪枝的累积效应