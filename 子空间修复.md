# 子空间修复（Subspace Repair）与 Delayed Repair：动机、现象与实现说明

本文档记录我们在 **LLaVA + token pruning（视觉 token 剪枝）** 场景下遇到的关键现象、叙事动机，以及一个可落地的“**只在任务有效子空间上修复 gap**”方案（低秩 / 子空间对齐）。

> 目标不是让 4096 维全空间上“完全一致”，而是让剪枝后的模型在**任务真正依赖的关键方向**上更接近 teacher / keep-all，从而提升下游准确率，同时避免在大量无关维度里引入漂移。

---

## 1. 我们的动机链条（论文故事）

1) **Fine-tuned pruning 方法为什么常常效果更好？**  
直觉上：剪枝会改变内部表征分布（domain shift / gap），fine-tuning 的收益之一就是“修复”这个 shift，使得模型在剪枝后仍保持与原模型相近的有效表示。

2) **我们观测到剪枝会引入 gap（distribution shift）**  
在一些层（例如我们选定的 `repair_layers`）上，剪枝后的 hidden states（fake）与 keep-all（real/teacher）存在明显分布差异。

3) **引入 adapter 做 Delayed Repair**  
不是在剪枝层立刻修，而是先剪（gap 暴露出来），再在后面的层对 gen_answer token 的表示做修复：  
`fake -> corrected` 更接近 `teacher`，从而让任务输出更稳。

4) **关键发现：全空间对齐指标可能变差，但任务指标可能变好**  
我们多次看到：
- LDA 1D 投影方向上：`corrected` 明显朝 `teacher` 移动（很符合“修复 gap”的故事）
- 但在 4096 维全空间的 MMD / SWD / Frechet / C2ST / token-wise L2 等指标上，`corrected` 反而更差
- hard_forward accuracy 仍可能略涨

这提示一个非常重要的解释：  
**任务性能受“少数关键子空间/判别方向”的影响更大**。  
只要在这些方向上更像 teacher，就可能提升准确率；与此同时，adapter 在大量“无关/正交子空间”里引入漂移，会让全局分布距离指标变差。

---

## 2. 术语与我们在代码里的三种表征（real/fake/corrected）

以某个 repair layer `Lk` 为例，我们在 gen_answer token 区域（成本低）定义：

- **teacher / real**：`pruning_mode=keep_all`（不剪枝），并且 `apply_repair=False`  
- **fake**：`pruning_mode=normal`（剪枝生效），并且 `apply_repair=False`  
- **corrected**：`pruning_mode=normal`，并且 `apply_repair=True`（插入 repair adapter 后的结果）

我们关心的 gap：

- **gap（目标修复方向）**：`gap = teacher_h - fake_h`
- **adapter delta（实际修复）**：`delta = corrected_h - fake_h`

注意：我们当前的 delayed repair 只在 **gen_answer tokens** 上生效，因此训练/可视化成本较低。

---

## 3. 为什么“全 4096 维对齐”会越修越差？

把 token 表征看成 `R^4096` 空间向量，任务相关信号往往不需要占满整个空间：

- 任务更敏感的方向（比如 LDA / logits 相关方向）可能是低维的
- 很多维度可能对输出不敏感（或只起到正则/冗余作用）

当我们用较强 repair 权重训练 adapter 时，adapter 很容易：

- 在“关键方向”上确实朝 teacher 移动（这解释了 LDA 图很漂亮、accuracy 可能微涨）
- 但同时在大量无关维度里产生漂移（全空间距离指标变差）

因此，我们希望一个结构性约束：

> **限制 adapter 的修复 delta 主要落在一个“任务有效/gap 主方向”的低秩子空间里。**

---

## 4. 子空间修复（Subspace Repair）：核心思想

### 4.1 子空间如何定义？

我们给一个可落地的近似定义：  
在 Stage2 开始时（pruner 已冻结、剪枝行为稳定），用少量样本估计 **gap 的主方向**：

1) 对每个 `repair_layer`，收集一批 token-wise gap 向量：
   - `gap_tokens = teacher_h - fake_h`（只取 gen_answer tokens）
2) 对 gap_tokens 做 PCA，取 top-r 主成分作为基：
   - 得到 `B ∈ R^{4096×r}`，列向量近似正交

这个 `span(B)` 就是我们要对齐/修复的“子空间”（近似“gap 的主子空间”）。

### 4.2 delta 如何被约束？

对于 repair adapter 的输出增量 `delta`，在 forward 时做投影：

- 平行分量：`delta_par = P_B(delta)`
- 正交分量：`delta_orth = delta - delta_par`

最终使用：

`delta' = delta_par + orth_scale * delta_orth`

其中：
- `orth_scale=0`：完全压制正交分量（最强约束）
- `orth_scale∈(0,1)`：部分保留正交分量
- `orth_scale=1`：等价于不约束（回到原方法）

---

## 5. 两步训练（Two-Step）与子空间校准放在哪？

我们采用 **真两步训练**：

### Stage 1：只训练 pruner
- 不插 repair adapter（稳定地学剪枝）
- 三阶段温度 / 噪声 / 稀疏度退火等，统一在 Stage1 完成
- 保存 `checkpoint_stage1_pruner_only.pt`

### Stage 2：冻结 pruner，只训练 repair adapter
- 训练环境稳定（pruner deterministic）
- 在 Stage2 **开始时**做一次子空间校准（rank0 计算 + broadcast）
- 然后 adapter 训练时 delta 会自动走子空间投影

这样做的好处：
- adapter 看到的 gap 更稳定（pruner 不再动）
- 校准出的子空间也更稳定，可复用到后续 checkpoint（便于对比）

---

## 6. 我们在代码里的实现（LLaVA only）

### 6.1 关键文件

- `method/models/subspace.py`：gap-PCA 与投影工具
- `method/models/prunable_llava.py`：在 repair delta 上应用子空间投影（仅 gen_answer tokens）
- `main_acp_ddp.py`：Stage2 开始时校准子空间基；checkpoint 存/取；DDP 广播
- `eval_acp_ddp.py` / `scripts/visualize_distribution_shift.py`：加载 checkpoint 时恢复子空间基（避免评估时丢失）

### 6.2 校准口径（next-LN vs pre-LN）

delta 是在 **pre-LN** 空间施加的，因此默认建议在 **pre-LN** 空间估计子空间（`repair_subspace_apply_next_layernorm: false`）。  
如果你希望更贴近可视化脚本常用的“下一层看到的空间”，也可以尝试打开 next-LN 口径，但这属于启发式（因为 LN 非线性，严格一致需要更复杂的处理）。

---

## 7. 评估：我们怎么判断“更接近 teacher”？

在训练日志里已经支持直接打印：
- 每层 `MSE(fake, teacher)` vs `MSE(corrected, teacher)` 的 improvement%
  - 日志字段：`RepairMSE: avg_imp=... [Lk(b=...,a=...,imp=...%)]`

可视化与分布诊断：
- `scripts/visualize_distribution_shift.py`：支持读取 checkpoint，画 PCA/t-SNE/LDA 等
  - **建议论文图**：LDA 1D 投影图非常直观，能支撑“关键子空间对齐”的叙事

任务指标：
- hard_forward accuracy 的变化可以作为最终参考（更贴任务）

---

## 8. 配置项（configs/vision_token_pruning.yaml）

子空间修复相关配置在 `method_settings` 下：

- `repair_subspace_enable`: 是否开启（建议先在 Stage2-only 实验里开）
- `repair_subspace_rank`: 子空间维度（例如 32/64/128）
- `repair_subspace_orth_scale`: 正交残差保留比例（0.0/0.1/0.2/...）
- `repair_subspace_calib_samples`: 校准用多少样本（越多越稳，越慢）
- `repair_subspace_calib_max_tokens_per_layer`: 控制 PCA token 数（防止内存爆）
- `repair_subspace_apply_next_layernorm`: 是否在 next-LN 空间估计（默认 false）
- `repair_subspace_recompute`: 如果 ckpt 已有 basis，是否强制重算

---

## 9. 推荐的消融与下一步

1) `rank` 扫描：`32 / 64 / 128`
2) `orth_scale` 扫描：`0.0 / 0.1 / 0.2 / 0.5`
3) 与 `repair_delta_orth_weight` 的对照：
   - 子空间投影是“结构性硬约束”（更强）
   - delta 正则是“软约束”（更灵活）
4) 目标不是让全空间距离都变好：
   - 我们更关心：关键子空间（LDA）方向更像 teacher + hard_forward acc 更好

